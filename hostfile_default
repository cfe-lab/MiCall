# Copy this file from hostfile_default to hostfile and then adjust the settings
# for your local environment.

# For a development workstation, uncomment this line. It will run all processes
# on the local machine.
#localhost

# For the production environment, uncomment these lines. Add any new machines,
# and adjust their *relative* processing power. The scheduler will walk through
# this list assigning n processes to each machine, and looping through the
# list until enough processes have been assigned or all the hosts have hit
# their max_slots.
#Bulbasaur slots=5
#n0 slots=5
#n1 slots=5
#n2 slots=7
#n3 slots=7
